stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - docker run --rm -v $(pwd):/code:ro -w /code --entrypoint=/code/ci/build.sh bluetab/amazonlinux-phoenix-postgresql:20180122121634065033384

test:
  stage: test
  script:
    - echo "Hello build"
  artifacts:
    untracked: true

deploy:
  stage: deploy
  script:
    - echo "Hello build"
  only:
    - master
